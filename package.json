{
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "npm run check:js && npm test",
    "check:js": "node --check frontend/config.js && node --check frontend/app.js && node --check api/blob-upload.js",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@vercel/blob": "2.4.1"
  }
}
